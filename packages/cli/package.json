{
  "name": "promptcode-cli",
  "version": "0.8.7",
  "description": "CLI tool for PromptCode - Generate AI-ready prompts from codebases",
  "bin": {
    "promptcode": "./dist/promptcode"
  },
  "scripts": {
    "build": "node scripts/inject-version.js && npm run typecheck && node scripts/generate-checksums.js && node scripts/embed-templates.js && bun build src/index.ts --compile --target=bun --outfile dist/promptcode && npm run copy-templates",
    "build:prod": "PROD_BUILD=1 npm run build",
    "copy-templates": "node -e \"const fs=require('fs'); const path=require('path'); fs.cpSync('src/claude-templates', 'dist/claude-templates', {recursive:true}); fs.cpSync('src/cursor-templates', 'dist/cursor-templates', {recursive:true})\"",
    "dev": "bun run src/index.ts",
    "clean": "rm -rf dist",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^2.0.45",
    "@ai-sdk/google": "3.0.0-beta.49",
    "@ai-sdk/openai": "^2.0.68",
    "@ai-sdk/xai": "^2.0.33",
    "@promptcode/core": "file:../core",
    "@types/inquirer": "^9.0.9",
    "ai": "6.0.0-beta.99",
    "chalk": "^5.6.2",
    "chokidar": "^4.0.3",
    "commander": "^14.0.2",
    "diff": "^8.0.2",
    "inquirer": "^13.0.1",
    "minimatch": "^10.1.1",
    "openai": "^6.9.1",
    "ora": "^9.0.0",
    "undici": "^7.16.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/diff": "^5.0.9",
    "@types/minimatch": "^5.1.2",
    "@types/node": "^20.0.0",
    "bun-types": "latest",
    "glob": "^12.0.0",
    "typescript": "^5.0.0"
  },
  "bun": {
    "test": {
      "concurrency": 4
    }
  }
}
