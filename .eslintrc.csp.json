{
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "JSXAttribute[name.name=/^on[A-Z]/]",
        "message": "Inline event handlers violate CSP. Use addEventListener instead."
      },
      {
        "selector": "TemplateLiteral > TemplateElement[value.raw=/\\bon[a-z]+\\s*=/i]",
        "message": "Inline event handlers in template literals violate CSP. Use addEventListener instead."
      },
      {
        "selector": "Literal[value=/\\bon[a-z]+\\s*=/i]",
        "message": "Inline event handlers in strings violate CSP. Use addEventListener instead."
      }
    ],
    "no-restricted-properties": [
      "error",
      {
        "object": "document",
        "property": "write",
        "message": "document.write can cause CSP violations. Use DOM methods instead."
      },
      {
        "property": "innerHTML",
        "message": "innerHTML with event handlers violates CSP. Ensure no inline handlers or use safer alternatives."
      }
    ]
  },
  "overrides": [
    {
      "files": ["src/webview/**/*.{ts,js}", "src/webviewProvider.ts"],
      "rules": {
        "no-eval": "error",
        "no-implied-eval": "error",
        "no-new-func": "error",
        "no-script-url": "error"
      }
    }
  ]
}